# sleeper
Baby sleep tracker

## Web

A React frontend lives in `web/` with routes for dashboard, logs, and settings. Install dependencies and start the dev server:

```
cd web
npm install
npm run dev
```
